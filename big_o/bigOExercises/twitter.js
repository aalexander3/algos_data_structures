// retrieve most recent tweet and very oldest tweet

// find first and find last
// O(1) - initial thought
// constant time
const findFirstAndLast = tweets => {
  tweets[0]
  tweets[tweet.length -1]
}


// compare dates of every tweet
//  tweet object with date and message

// O(n^2)
// compare every tweet against each other one
// nested loops
// expensive / innefficient


// length could be a lookup like in js
// or it could be iterating over each char and counting up
